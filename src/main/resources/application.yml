#Tomcat服务器配置
server:
    port: 8080
    servlet:
        context-path: /

#数据库配置
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/study?CharacterEncoding=utf8&useSSL=true&serverTimezone=UTC
        password: youpinchailv
        driver-class-name: com.mysql.jdbc.Driver
        username: root
mybatis:
    mapper-locations: classpath：mapper/.*
